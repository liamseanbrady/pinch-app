<%= render 'shared/header', title: 'Create a New Category' %>

<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br/>
  <%= f.submit 'Create Category' %>
<% end %>