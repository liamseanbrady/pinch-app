<%= render 'shared/header', title: "#{@category.name}" %>

<div class="row">
  <% @category.goals.each do |goal| %>
    <div class="col-lg-3 col-md-4 col-sm-6">
      <%= render 'goals/goal', goal: goal %>
    </div>
  <% end %>
</div>